import React from 'react'

export default function OverviewGraph() {
  return (
    <div
      style={{
        height: "60px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <p style={{ color: "white" }}>OverviewGraph</p>
    </div>
  );
}
